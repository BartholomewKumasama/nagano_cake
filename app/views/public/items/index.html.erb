<div class="container">
    <div class="row">
        <div class="col-md-2 bg-dark text-white"style="margin-top: 50px;">
          
          ジャンル機能<br/>建設予定地
          <br/>
          <br/>
          のちに追記します
        </div>
           <div class="col-md-10" style="margin-top: 50px;">
             
              <h4>商品一覧  　<span style="font-size:20px;">(全<span style="font-size:25px;"><%= @items.count %></span>件)</span></h4>
              <div class="d-flex flex-wrap">
              <% @items.each do |item| %>
               <div class ="item_container px-5 py-5" style = "width: 25%">
                <%= link_to public_item_path(item.id) do %>
                  <%= image_tag item.get_image, size: '150x150' %>
                <% end %>
                  <p><%= item.name %><br />
                  税込み <%= (item.add_tax_price.to_s(:delimited)) %>円</p>
                  </div>
              <% end %>
            </div>
             
        </div>
    </div>
</div>