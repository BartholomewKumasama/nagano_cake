<p id="notice"></p>
<div class="row">
    <div class="jumbotron col-md-8 offset-md-2">
      <%= image_tag 'cake.jpg', size: '480x300' %>
      <%#　cssで↓の文の背景にする設定しないといけない%>
<div>
  <p>
    ようこそ、ながのCAKEへ！</br>
    このサイトは、ケーキ販売のECサイトになります。</br>
    会員でない方も商品の閲覧はできますが</br>
    購入には会員登録が必要になります。
  </p>
</div>


<h2>新着商品</h2>

  <% @items.first(4).each do |item| %>
   <div class ="col-md-4">
     <ul class="list-inline">
         <% if item.item_image.attached? %>
           <%= link_to item_path(item.id) do %>
             <%= image_tag item.get_item_image(180,180) %>
           <% end %>
         <% else %>
            <%= link_to item_path(item.id) do %>
              <%= image_tag 'no_image', size: "180x180" %>
            <% end %>

         <%= item.name %><br>
     ￥<%= (item.price).to_s(:delimited) %><br><%#上手く設定が出来ていない可能性有%>
     </ui>
   </div>
  <% end %>
 <% end %>
  <div class="text-right mr-5 mt-5">
    <%= link_to '全ての商品を見る >', public_items_path %>
  </div>


