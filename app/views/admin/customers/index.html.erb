<div class="container" style="margin-top: 50px;">
  <div class="col-md-9 offset-md-1"> <%#.offset-md-1 は .col-md-9 を 1 カラムぶん右に移動%>
<div class="shadow p-3 mb-5 bg-white rounded">

  <h2 class="p-3 mb-2 bg-dark text-white">会員一覧</h2>
  <p id="notice"></p>
  <table class="table table-hover">
    <thead>
      <tr class="table-secondary">
        <th>会員ID</th>
        <th>氏名</th>
        <th>メールアドレス</th>
        <th>ステータス</th>
        <th colspan="5"></th><br>
      </tr>
    </thead>
      <% @customers.each do |customer| %><%# @publics = Public.allですべてのPublicユーザーの情報を取得して、順番に並べて表示 %>
    <tbody>
      <tr>
      <td><%= customer.id %></td>

      <td>
        <%# A+Bと表記して、二つの表示をくっつけてABと表示できます。%>
        <%= link_to customer.family_name+customer.first_name, admin_customer_path(customer.id) %>
      </td>

      <td><%= customer.email %></td>

      <td>
        <% if customer.is_deleted %><%# is_deletedの情報を取得して退会済みかチェック %>
          <b class="text-secondary">退会</b>  <%# true(退会済み)の時の表示 %>
        <% else %>
          <b class="text-success">有効</b>  <%# false(初期値)の時の表示 %>
        <% end %>
      </td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>


</div>
</div>