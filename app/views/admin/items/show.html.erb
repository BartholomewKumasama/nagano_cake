<div class="container">
  <div class="row">
    <div class="col-md-3">
　　　<table class="table">
      <h2 class="text-left">商品詳細</h2>
      <tr>
      <% if @item.image.attached? %>
        <%= image_tag @item.image, size: "200x200" %>
      <% else %>
        <%= image_tag 'no_image', size: "200x200" %>
      <% end %>
      </tr>
     </table>
      </div>
    <div class="col-md-9">
    <table class="table">
      <tbody>
      <tr><td>商品名</td><td><%= @item.name %></td></tr>
      <tr><td>商品説明</td><td><%= @item.introduction %></td></tr>
        <tr><td>ジャンル</td><td><%= @item.genre.name %></td></tr>
          <tr>
            <td> 税込価格<br> (税抜価格) </td>
            <td>
              <%= @item.add_tax_price.to_s(:delimited) %>(
              <%= @item.price.to_s(:delimited) %>)円
            </td>
          </tr>

          <tr>
            <td>販売ステータス</td>
            <td class="font-weight-bold text-success">
              <% if @item.is_active %><%# is_activeの情報を取得して在庫があるかチェック %>
              販売中<%# trueの表示がでる %>
              <% else %>
              販売停止中<%# falseの表示がでる %>
              <% end %>
            </td>
          </tr>
          <tr>
            <td><%= link_to '編集する', edit_admin_item_path(@item.id), class: "btn btn-success" %></td>
            <td><%= link_to "一覧に戻る", admin_items_path, class: "btn btn-success" %></td>  <%#　なんとなく不便だったのでつけました %>
          </tr> 
      </tbody>
    </table>
    </div>
  </div>
</div>